<% if resource.errors.any? %>
    <% resource.errors.each do |error| %>
      <%= render "shared/flash",
        error: true,
        notice: "#{error.full_message}"
      %>
    <% end %>
  <% end %>
